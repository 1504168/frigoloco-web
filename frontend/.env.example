# Base URL of the FrigoLoco FastAPI backend.
# The typed API client (src/lib/api.ts) reads this; it defaults to the value
# below when unset. Copy this file to `.env.local` and adjust as needed.
VITE_API_BASE_URL=http://localhost:8100
