"""Routers package.

Placeholder: individual router modules are contributed by other agents. Each such
module is expected to expose a module-level ``router`` (an ``APIRouter``), which
``app.main`` auto-discovers and includes. The application boots fine when this
package is empty.
"""
